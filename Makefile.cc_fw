ifeq ($(MAKECMDGOALS),cc_fw)

DEFINES+=-DSL_PLATFORM_MULTI_THREADED
DEFINES+=-DUSE_FREERTOS

# Where to find source files that do not live in this directory.
VPATH=${ROOT}/cc_fw
VPATH+=${ROOT}/common
VPATH+=${ROOT}/driverlib
VPATH+=${ROOT}/simplelink/source
VPATH+=${ROOT}/simplelink
VPATH+=${RTOS_SOURCE_DIR}
VPATH+=${RTOS_SOURCE_DIR}/source
VPATH+=${RTOS_SOURCE_DIR}/source/portable/MemMang
VPATH+=${RTOS_SOURCE_DIR}/source/portable/GCC/ARM_CM4
VPATH+=${ROOT}/oslib
VPATH+=${ROOT}/firmware
VPATH+=${ROOT}/library

# Where to find header files that do not live in the source directory.
IPATH=${ROOT}
IPATH+=${ROOT}/cc_fw
IPATH+=${ROOT}/common
IPATH+=${ROOT}/inc
IPATH+=${ROOT}/driverlib
IPATH+=${ROOT}/oslib
IPATH+=${ROOT}/simplelink
IPATH+=${ROOT}/simplelink/source
IPATH+=${ROOT}/simplelink/include
IPATH+=${ROOT}/firmware
IPATH+=${RTOS_SOURCE_DIR}
IPATH+=${RTOS_SOURCE_DIR}/source
IPATH+=${RTOS_SOURCE_DIR}/source/include
IPATH+=${RTOS_SOURCE_DIR}/source/portable/MemMang
IPATH+=${RTOS_SOURCE_DIR}/source/portable/GCC/ARM_CM4
IPATH+=${ROOT}/library

# WyLight firmware
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/main.o
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/pinmux.o
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/CC3200_Platform.o
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/BroadcastTransmitter.o
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/CPPTask.o
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/SimplelinkServers.o
# ${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/wy_network_if.o
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/SimplelinkDriver.o
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/wifi.o
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/pwm.o
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/wy_spi.o
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/RingBuf.o
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/crc.o
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/CommandIO.o
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/wy_firmware.o
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/Version.o
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/wy_eeprom.o
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/ScriptCtrl.o
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/ledstrip.o
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/wy_crc.o
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/BroadcastMessage.o
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/wy_Timer.o
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/wy_Pwm.o
# common
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/startup_gcc.o
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/uart_if.o
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/gpio_if.o
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/wy_fs.o

#driverLib
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/cpu.o
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/flash.o
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/interrupt.o
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/pin.o
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/prcm.o
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/uart.o
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/utils.o
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/timer.o
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/udma.o
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/spi.o
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/gpio.o
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/crc.o

# simplelink
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/socket.o
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/fs.o
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/device.o
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/wlan.o
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/driver.o
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/netcfg.o
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/cc_pal.o
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/flowcont.o
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/netapp.o

#${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/tmp006drv.o
#${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/bma222drv.o

${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/timer_if.o
#${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/gpio_if.o
#${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/uart_if.o
#${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/udma_if.o

# freeRTOS
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/croutine.o
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/list.o
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/queue.o
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/tasks.o
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/port.o
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/timers.o
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/heap_3.o
${BINDIR}/${PRJ_NAME}.elf: ${OBJDIR}/osi_freertos.o

endif
